import { UserForDisplay } from "../../../administration/user-management/user";

const UserDetailPage = ({}: {
  users?: UserForDisplay[];
  onSave?: (data: UserForDisplay) => void;
}) => {
  return <div className=""></div>;
};

export default UserDetailPage;
